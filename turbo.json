{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".turbo", "dist"]
    },
    "dev": {
      "cache": false
    }
  }
}
